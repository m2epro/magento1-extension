ConfigurationGeneralHandler = Class.create();
ConfigurationGeneralHandler.prototype = Object.extend(new CommonHandler(), {

    // ---------------------------------------

    initialize: function(synchProgressObj, storedStatuses)
    {

    }

    // ---------------------------------------
});